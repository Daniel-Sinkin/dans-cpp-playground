# dans-template

Starter C++ project template for my personal projects.

This template uses `dans-core` for shared foundation headers. CMake prefers a local
sibling checkout at `../dans-core` and otherwise fetches `dans-core` at `v0.2.0`.
